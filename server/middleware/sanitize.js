/**
* @todo sanitize user input coming from the frontend
*/
const sanitize= (req, data)=> {
  console.log('\n\nrequest has passed JOI VALIDATION')
  console.log('CHORE: ADD DOMPURIFY')
}

module.exports = {sanitize}
